apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-service
  namespace: healthcare-platform
  labels:
    app: audit-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: audit-service
  template:
    metadata:
      labels:
        app: audit-service
    spec:
      containers:
        - name: audit-service
          image: healthcare-platform/audit-service:latest
          ports:
            - containerPort: 8088
          envFrom:
            - configMapRef:
                name: platform-config
          env:
            - name: DB_HOST
              value: "postgres.healthcare-platform.svc.cluster.local"
            - name: DB_NAME
              value: "audit_db"
            - name: DB_USER
              value: "audit_user"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: JWT_SECRET
            - name: KAFKA_SASL_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: platform-config
                  key: KAFKA_SASL_USERNAME_AUDIT
            - name: KAFKA_SASL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: KAFKA_SASL_PASSWORD_AUDIT
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8088
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8088
            initialDelaySeconds: 30
            periodSeconds: 15
          resources:
            requests:
              cpu: "250m"
              memory: "384Mi"
            limits:
              cpu: "1000m"
              memory: "768Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: healthcare-platform
spec:
  selector:
    app: audit-service
  ports:
    - port: 8088
      targetPort: 8088
  type: ClusterIP
