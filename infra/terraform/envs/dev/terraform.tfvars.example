# Copy to terraform.tfvars (gitignored) and fill in.

location = "eastus"

# Your workstation/office/VPN egress IP(s), as /32s, e.g. curl -s ifconfig.me
admin_ip_cidrs = [
  "203.0.113.4/32",
]

tags = {
  project   = "healthcare-platform"
  managedby = "terraform"
  owner     = "CHANGEME"
}
